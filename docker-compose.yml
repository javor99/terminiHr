version: '3.9'
services:
  
  db:
    image: "postgres"
    ports:  ["5434:5432"]
    environment: 

        "POSTGRES_PASSWORD": "root"
        "POSTGRES_USER": "postgres"
        "POSTGRES_DB":  "Termini"

  server:
    build: .
    ports:  ["3000:3000"]
    depends_on:
      - db